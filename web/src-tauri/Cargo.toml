[package]
name = "skillhub-desktop"
version = "0.1.0"
description = "SkillHub desktop client — one-click install skills to local agents"
edition = "2021"
rust-version = "1.75"

[lib]
name = "skillhub_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Filesystem home resolution (cross-platform macOS / Windows)
dirs = "5"
# HTTP download of skill zips from the registry.
# `system-proxy` is required: `default-features = false` also drops the OS proxy
# integration, so without it a download behind a corporate/macOS system proxy
# fails with no actionable error.
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls", "json", "system-proxy"] }
# Cross-platform zip extraction (no dependency on the system `unzip`)
zip = { version = "2", default-features = false, features = ["deflate"] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
